#include<stdio.h>
int main()
{
	int a,b,k;
	printf("Enter the two numbers",a,b);
	scanf("%d%d",&a,&b);
    switch(a>b)
	{
		case 0:
		printf("b is the maximum number",a);
		break;
	    default:printf("a is the maximum number"); 
	
	}

}
	
