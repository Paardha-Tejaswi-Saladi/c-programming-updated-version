#include<stdio.h>
main()
{
	int character,vowel,consonant,a,e,i,o,u;
	printf("Enter a character",character);
	scanf("%d",&character);
	if(character==a||e||i||o||u)
	{
	    printf("character is an vowel",vowel);
    }
	else
	{
		printf("character is a consonant",consonant);
	}
}
